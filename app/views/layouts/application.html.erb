<!DOCTYPE html>
<html>
  <head>
    <title>LeRorTwitter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
    <header>
      <div><%= link_to "Traveler", root_path, class: "text-white" %></div>
      <div><%= link_to "トップ", root_path, class: "text-white" %></div>
      <div><%= link_to "投稿", new_post_path, class: "text-white" %></div>
      <div><%= link_to "投稿一覧", posts_path, class: "text-white" %></div>
      <div><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-white" %></div>
    </header>
    <% end %>

    <%= yield %>
  </body>
</html>
